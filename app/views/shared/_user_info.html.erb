<div class="user">
  <% if current_user.avatar? %>
    <%= image_tag current_user.avatar.url %>
  <% else %>
    <%= image_tag "default-avatar.png"%>
  <% end %>
  <h3><%= current_user.name %></h3>
  <span><%= link_to t("view_profile") , current_user %></span>
</div>
